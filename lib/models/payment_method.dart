/// The `PaymentMethod` class in Dart represents a payment method with a name and an icon path.
class PaymentMethod {
  final String name;
  final String iconPath;

  const PaymentMethod({
    required this.name,
    required this.iconPath,
  });
}